import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.photostudio.manager',
  appName: 'PhotoStudio Manager',
  webDir: 'desktop/dist'
};

export default config;
